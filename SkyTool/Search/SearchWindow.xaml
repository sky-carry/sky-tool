<Window x:Class="SkyTool.Search.SearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="文件搜索 - Sky 工具箱" Height="600" Width="780" MinWidth="520" MinHeight="360"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgBrush}" FontFamily="Segoe UI">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 搜索框 -->
        <Border Grid.Row="1" Margin="14,2,14,0" Background="{StaticResource SurfaceBrush}"
                CornerRadius="10" BorderThickness="1" BorderBrush="{StaticResource StrokeBrush}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="&#xE721;" FontFamily="Segoe MDL2 Assets"
                           FontSize="15" Foreground="{StaticResource FaintBrush}"
                           Margin="14,0,0,0" VerticalAlignment="Center"/>
                <TextBox x:Name="SearchBox" FontSize="17" Padding="10,10"
                         Background="Transparent" Foreground="{StaticResource TextBrush}"
                         BorderThickness="0" CaretBrush="{StaticResource AccentBrush}"
                         TextChanged="SearchBox_TextChanged" KeyDown="SearchBox_KeyDown"/>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" x:Name="AdminBanner" Background="#33F5C97B" CornerRadius="8"
                Padding="12,8" Margin="14,10,14,0" Visibility="Collapsed">
            <DockPanel>
                <Button DockPanel.Dock="Right" Style="{StaticResource PillBtn}"
                        Background="{StaticResource AmberBrush}" Foreground="#1E1E2E"
                        Content="以管理员重启" FontSize="12" Padding="10,4"
                        Click="RestartAsAdmin_Click"/>
                <TextBlock Foreground="{StaticResource AmberBrush}" VerticalAlignment="Center" FontSize="12"
                           TextWrapping="Wrap" Margin="0,0,10,0"
                           Text="当前为普通模式（目录扫描）。以管理员运行可启用 MFT 秒级索引与实时更新。"/>
            </DockPanel>
        </Border>

        <!-- 结果列表 -->
        <ListBox x:Name="ResultList" Grid.Row="3" Margin="8,8,8,0"
                 Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                 MouseDoubleClick="ResultList_MouseDoubleClick" KeyDown="ResultList_KeyDown">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Focusable" Value="True"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd" Background="Transparent" CornerRadius="8"
                                        Padding="10,7" Margin="6,1">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentDimBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Left" Text="{Binding Icon}" FontSize="18"
                                   VerticalAlignment="Center" Margin="2,0,10,0"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextBrush}"
                                       FontSize="13.5" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding FullPath}" Foreground="{StaticResource FaintBrush}"
                                       FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,1,0,0"/>
                        </StackPanel>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="打开" Click="Open_Click"/>
                    <MenuItem Header="打开所在文件夹" Click="OpenFolder_Click"/>
                    <MenuItem Header="复制完整路径" Click="CopyPath_Click"/>
                </ContextMenu>
            </ListBox.ContextMenu>
        </ListBox>

        <TextBlock x:Name="StatusText" Grid.Row="4" Margin="18,8,18,12"
                   Foreground="{StaticResource FaintBrush}" FontSize="11" Text="准备就绪"/>
    </Grid>
</Window>
