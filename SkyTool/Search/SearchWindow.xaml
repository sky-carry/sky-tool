<Window x:Class="SkyTool.Search.SearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="文件搜索 - Sky 工具箱" Height="560" Width="860"
        WindowStartupLocation="CenterScreen"
        Background="#FF1E1E2E">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="SearchBox" Grid.Row="0" FontSize="18" Padding="10,8"
                 Background="#FF2A2A3E" Foreground="White" BorderBrush="#FF44475A"
                 CaretBrush="White" TextChanged="SearchBox_TextChanged"
                 KeyDown="SearchBox_KeyDown"/>

        <Border Grid.Row="1" x:Name="AdminBanner" Background="#FF7A5C00" CornerRadius="4"
                Padding="10,6" Margin="0,8,0,0" Visibility="Collapsed">
            <DockPanel>
                <Button DockPanel.Dock="Right" Content="以管理员身份重启" Padding="10,3"
                        Click="RestartAsAdmin_Click"/>
                <TextBlock Foreground="White" VerticalAlignment="Center"
                           Text="当前为普通模式（目录扫描）。以管理员运行可启用 MFT 秒级索引与实时更新。"/>
            </DockPanel>
        </Border>

        <ListView x:Name="ResultList" Grid.Row="2" Margin="0,10,0,0"
                  Background="#FF2A2A3E" Foreground="#FFE0E0E0" BorderBrush="#FF44475A"
                  VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                  MouseDoubleClick="ResultList_MouseDoubleClick" KeyDown="ResultList_KeyDown">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="名称" Width="300">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Icon}" Margin="0,0,6,0"/>
                                    <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="路径" Width="500">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding FullPath}" Foreground="#FF9DA3B4" TextTrimming="CharacterEllipsis"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="打开" Click="Open_Click"/>
                    <MenuItem Header="打开所在文件夹" Click="OpenFolder_Click"/>
                    <MenuItem Header="复制完整路径" Click="CopyPath_Click"/>
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>

        <TextBlock x:Name="StatusText" Grid.Row="3" Margin="2,8,2,0"
                   Foreground="#FF9DA3B4" Text="准备就绪"/>
    </Grid>
</Window>
