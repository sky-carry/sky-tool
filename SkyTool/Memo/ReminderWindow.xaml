<Window x:Class="SkyTool.Memo.ReminderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="360" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize" ShowActivated="True"
        FontFamily="Segoe UI">
    <Grid Margin="12">
        <Border CornerRadius="12" Background="#FA232336" BorderBrush="{StaticResource AmberBrush}"
                BorderThickness="1.5" Padding="18,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="18" ShadowDepth="3" Opacity="0.55" Color="Black"/>
            </Border.Effect>
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Border Width="36" Height="36" CornerRadius="10" Background="#33F5C97B">
                        <TextBlock Text="⏰" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="11,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="任务提醒" Foreground="{StaticResource AmberBrush}" FontSize="13" FontWeight="SemiBold"/>
                        <TextBlock x:Name="TimeText" Foreground="{StaticResource SubBrush}" FontSize="11" Margin="0,1,0,0"/>
                    </StackPanel>
                </StackPanel>
                <TextBlock x:Name="TaskText" Foreground="{StaticResource TextBrush}" FontSize="15" FontWeight="SemiBold"
                           TextWrapping="Wrap" Margin="1,12,0,16"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource PillBtn}" Background="{StaticResource GreenBrush}"
                            Foreground="#16291B" Content="✓ 完成" FontWeight="SemiBold"
                            Margin="0,0,8,0" Click="Done_Click"/>
                    <Button Style="{StaticResource PillBtn}" Content="5 分钟后" Margin="0,0,8,0" Click="Snooze_Click"/>
                    <Button Style="{StaticResource PillBtn}" Content="知道了" Click="Dismiss_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
